// eslint-disable-next-line import/prefer-default-export
export enum ContentPath {
  HOME = '/',
  DICTIONARY = '/dictionary',
  EXPLORER = '/explore',
  GAME = '/game',
  TIMER = '/timer',
  ANALYTICS = '/analytics',
}
